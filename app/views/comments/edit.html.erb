<h2>Edit Comment</h2>

<%= form_with(model: [@film, @comment], method: :patch) do |form| %>
  <div class="form-group">
    <%= form.label :text %>
    <%= form.text_area :text, class: "form-control" %>
  </div>

  <%= form.submit "Update Comment", class: "btn btn-primary" %>
<% end %>